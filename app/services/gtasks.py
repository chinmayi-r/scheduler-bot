from __future__ import annotations
from typing import List

def add_todo_stub(text: str) -> None:
    # Replace with Google Tasks API later
    return

def list_todos_stub() -> list[tuple[str, bool]]:
    # (text, is_done)
    return []
